import { BaseEndpoint } from "./base";
import { Search } from "..";
export declare namespace SearchPaths {
    const saved: string;
}
export declare class SearchEndpoint extends BaseEndpoint {
    saved(): Promise<Search[]>;
}
